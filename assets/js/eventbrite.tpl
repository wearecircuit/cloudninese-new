			<div class="figure-wrap">
				<figure>
					<time datetime="2014-05-14">
						<span class="date">{{= it.date}}</span>
						<span class="month">{{= it.month}}</span>
					</time>
					<img src="{{= it.logo}}" alt="">
				</figure>
			</div>

			<article>
				<p><i class="cn cn-pink-rabbit"></i>{{= it.format}}</p>
				<h2><a href="{{= it.url1}}">{{= it.title}}</a></h2>
				<span>
					<a href="{{= it.url1}}" class="btn">Läs mer och anmäl dig</a>
					<a href="{{= it.url2}}">Se fler aktuella</a>
				</span>
			</article>